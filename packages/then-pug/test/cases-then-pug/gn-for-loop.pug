ul
  - var dirs = yield readdir('cases-then-pug/samples')
  for file in dirs
    - var stats = yield stat('cases-then-pug/samples/' + file)
    if stats.isFile()
      li= file + " is a file"
    if stats.isDirectory()
      li= file + " is a directory"
